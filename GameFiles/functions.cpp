
//includes
#include "functions.h"

/*
	Implementation file for functions.h with functions definitions in the 
	namespace zsk tailored to this game
*/

namespace zsk {

	

	sf::Vector2f randomSpawn(const sf::Vector2f & range_x, const sf::Vector2f & range_y)
	{
		float rand_x;
		float rand_y;

		return sf::Vector2f(rand_x, rand_y);

	}

	float distanceFrom(sf::Vector2f & loc1, sf::Vector2f & loc2)
	{
		return 0.0f;
	}

}